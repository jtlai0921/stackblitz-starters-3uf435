/**
 * 定義需要登出的error code
 * error code 來自 行動中台回傳
 */
export const ERRORCODE_LIST_LOGOUT = [

];

/**
 * 定義需要控制版本的錯誤訊息
 */
export const ERRORCODE_LIST_APPVERSION = {
    // 需要更新的error code
    VERSION_UPDATE: []
};